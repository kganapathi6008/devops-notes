# Module 2 – Variables, Outputs and Expressions

This module explains how to make Terraform configurations dynamic, reusable, and flexible using variables, outputs, and expressions.

---

# 1. Input Variables

Input variables allow you to pass values into your Terraform configuration instead of hardcoding them.

Without variables:

```hcl
instance_type = "t3.micro"
```

With variables:

```hcl
variable "instance_type" {
  type = string
}

resource "aws_instance" "web" {
  ami           = "ami-1234567890"
  instance_type = var.instance_type
}
```

Usage:

```
terraform apply -var="instance_type=t3.small"
```

Why this is useful:

* Same configuration can be reused with different values
* No need to modify code for small changes
* Easier environment customization

---

# 2. Variable Types and Validation

Terraform supports multiple data types.

## String

```hcl
variable "environment" {
  type = string
}
```

## Number

```hcl
variable "instance_count" {
  type = number
}
```

## Boolean

```hcl
variable "enable_monitoring" {
  type = bool
}
```

## List

```hcl
variable "subnet_ids" {
  type = list(string)
}
```

## Map

```hcl
variable "tags" {
  type = map(string)
}
```

Example usage:

```hcl
tags = var.tags
```

## Validation

You can restrict allowed values.

```hcl
variable "environment" {
  type = string

  validation {
    condition     = contains(["dev", "test", "prod"], var.environment)
    error_message = "Environment must be dev, test, or prod."
  }
}
```

Why validation is useful:

* Prevents incorrect inputs
* Reduces configuration mistakes
* Enforces standards

---

# 3. tfvars Files

Instead of passing variables manually, you can define them in a file.

Create a file:

```
terraform.tfvars
```

Example:

```hcl
instance_type  = "t3.micro"
environment    = "dev"
instance_count = 2
```

You can also create environment-specific files:

```
dev.tfvars
prod.tfvars
```

Apply using:

```
terraform apply -var-file="dev.tfvars"
```

Why this is useful:

* Separate configurations for different environments
* Cleaner command usage
* Organized variable management

---

# 4. Output Values

Outputs display useful information after infrastructure creation.

Example:

```hcl
output "instance_id" {
  value = aws_instance.web.id
}
```

Another example:

```hcl
output "public_ip" {
  value = aws_instance.web.public_ip
}
```

After apply, Terraform shows:

```
public_ip = 54.xx.xx.xx
```

Why outputs are useful:

* Retrieve generated values
* Pass values to other modules
* Display important information after deployment

---

# 5. Sensitive Outputs

If output contains sensitive information, mark it as sensitive.

```hcl
output "db_password" {
  value     = "example-password"
  sensitive = true
}
```

Terraform will hide the value in console output.

Why this is useful:

* Protects secrets
* Prevents accidental exposure
* Improves security practices

---

# 6. Expressions

Expressions allow dynamic computation instead of fixed values.

## Reference Expression

```hcl
subnet_id = aws_subnet.public.id
```

## Arithmetic Expression

```hcl
count = var.instance_count + 1
```

## Logical Expression

```hcl
enabled = var.enable_feature && var.environment == "prod"
```

Why expressions are useful:

* Make configuration flexible
* Reduce repetition
* Enable conditional behavior

---

# 7. Built-in Functions

Terraform provides many built-in functions.

## String Functions

```hcl
upper("terraform")
lower("DEV")
```

## Length Function

```hcl
length(var.subnet_ids)
```

## Contains Function

```hcl
contains(["dev", "test"], var.environment)
```

Functions help:

* Transform data
* Validate inputs
* Process lists and maps

---

# 8. Conditional Expressions

Conditional expressions use the ternary operator.

```hcl
instance_type = var.environment == "prod" ? "t3.medium" : "t3.micro"
```

Meaning:

If environment is prod, use larger instance.
Otherwise, use smaller instance.

Another example:

```hcl
count = var.create_instance ? 1 : 0
```

This creates a resource only if condition is true.

Why conditionals are useful:

* One configuration for multiple environments
* Controlled resource creation
* Cost optimization through dynamic sizing

---

# Module 2 Summary

After completing this module, you should be able to:

* Define and use input variables
* Apply validation rules
* Use tfvars files effectively
* Create meaningful outputs
* Protect sensitive data
* Write dynamic expressions
* Use built-in functions
* Apply conditional logic in Terraform
